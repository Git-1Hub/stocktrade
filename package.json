{
  "name": "fiduciary-accounting",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node src/generate-schedules.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}